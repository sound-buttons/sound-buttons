import { ButtonFilterPipe } from './button-filter.pipe';

describe('ButtonFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
